---
layout: post
{% if category -%}
tags:
{% for cat in category -%}
- {{ cat }}
{% endfor -%}
{% endif -%}
{% if slug -%}
permalink: {{ slug }}
{% endif -%}
date: {{ published }}
---
{% if photo -%}
  {% for pic in photo -%}
    ![{{ pic["alt"] }}]({{ pic["url"] }})
  {% endfor -%}
{% endif %}
{{ content }}
