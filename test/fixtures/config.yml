sites:
  testsite:
    github_repo: "lildude/micropub-github-pages"
    permalink_style: "/:categories/:year/:month/:title"
    site_url: "https://example.com"
    full_image_urls: false # Include site_url in img paths
    image_dir: "img"

micropub_token_endpoint: "http://example.com/micropub/token"

download_photos: true

syndicate_to_bridgy: true
